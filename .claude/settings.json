{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | grep -qE '\\.env($|\\.(local|prod|production|staging|development|test))' && echo '{\"decision\":\"block\",\"reason\":\"Direct .env edits are blocked to protect secrets. Update apps/backend/.env.example instead, then copy to .env\"}' || true"
          }
        ]
      }
    ]
  }
}
